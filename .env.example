# Telegram bot token for delivery in agents/orchestrator/delivery.ts.
# Required when classifications with severity=action_required or warning have
# a customer with telegram_chat_id set. Without this, sendTelegram throws and
# delivery counts errors; severity=info classifications are skipped regardless.
# Skapa via @BotFather i Telegram.
TELEGRAM_BOT_TOKEN=
