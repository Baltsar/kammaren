# Anthropic API key for LLM-tagging fallback in agents/orchestrator/rules/llm-tagger.ts.
# Required when classifier.runClassifier() encounters events keyword-tagged as ['okand'].
# Without this, LLM-fallback returns ['okand'] with method='llm-okand' and the pipeline continues.
ANTHROPIC_API_KEY=
